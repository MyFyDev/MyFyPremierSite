node_modules/*
*.swp
*.swo
.git/*

# only encrypted envs allowed — .env.keys holds the plaintext private keys and
# must never be committed
.env*
**/.env*
!/.env
!/.env.production

keys/*
logs/*
scratch/*
*.csv
